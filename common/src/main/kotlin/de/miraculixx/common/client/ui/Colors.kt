package de.miraculixx.common.client.ui

/** The gray vanilla's own world list uses for its two sub-lines. */
const val SUBTEXT_COLOR = -8355712

/** The wash every hand hit-tested row gets while the cursor is over it. */
const val HOVER_COLOR = 0x30FFFFFF

const val PANEL_BACKGROUND_COLOR = 0x8D000000.toInt()

const val PANEL_BORDER_COLOR = 0xFF505050.toInt()
