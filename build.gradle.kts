// Every module builds through the `de.miraculixx.fabric-mod` / `de.miraculixx.mod-publish`
// conventions in buildSrc. Nothing is applied here — loom must not touch the root project.

group = property("group") as String
